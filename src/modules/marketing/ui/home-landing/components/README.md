# Home Landing Components

Private view components for the marketing home landing slice live here. They are not imported
outside this slice; promote them to `src/shared/ui` only after a real shared contract appears.
